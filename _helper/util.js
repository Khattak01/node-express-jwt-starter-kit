
// const insertAt = (arr, index, newItem) => [
//     ...arr.slice(0, index),

//     newItem,

//     ...arr.slice(index)//  const newArr = insert(arr, 2, {name: 'ace'}); usage
// ];

// module.exports = { insertAt };